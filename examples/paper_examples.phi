// Examples for A message-passing interpretation of adjoint logic

// Example 1

type A = 1 * 1
type B = 1
assuming x1 : A * B 
prc[ex1] : B * A = <y, x2> <- recv x1; send self<x2, y>

// prc[ex1] : B * A = <y, x2> <- recv x1; send self<x2, y>